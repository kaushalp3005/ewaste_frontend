# Dev only: where Vite proxies /api requests (your local or Render backend)
VITE_API_URL=http://localhost:3000

# Client build-time keys (baked into the bundle at build time)
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_MAPS_API_KEY=
VITE_PUBLIC_BUILDER_KEY=
